<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/sliderbg"
    tools:context="com.ikolilu.ikolilu.portal.ui.dashboardFragments.actions.WardSuccessActivity">

    <ImageView
        android:id="@+id/email"
        android:layout_width="300dp"
        android:layout_height="200dp"
        android:layout_marginTop="20dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:foregroundGravity="center_horizontal"
        android:src="@drawable/email" />


    <TextView
        android:id="@+id/message"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:textAlignment="center"
        android:layout_below="@id/email"
        android:textStyle="bold"

        android:text="Ward Successfully Added. You can check your email to verify your ward Info."/>

    <Button
        android:id="@+id/go_to_dash"
        android:layout_width="300dp"
        android:layout_height="50dp"
        android:layout_marginTop="30dp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/round_thick_rect"
        android:layout_below="@id/message"
        android:text="verify email"
        android:textColor="@color/colorWhite"
        />
</RelativeLayout>
